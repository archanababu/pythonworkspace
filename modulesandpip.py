#----------------------------------
# Reusing functions in library file
# Go through module index in python
# pip - is a package manager. Used to install external module files
# pip install module-name
# pip uninstall module-name
#----------------------------------

import library

print(library.roll_dice(7))
